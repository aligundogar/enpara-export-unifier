PyMuPDF>=1.24      # PDF metin + render (fitz)
pytesseract>=0.3.10 # tesseract OCR sarmalayıcı (kredi kartı açıklamaları için)
Pillow>=10.0       # görüntü kırpma
pandas>=2.0        # XLS okuma + Excel yazma
openpyxl>=3.1      # .xlsx yazma
xlrd>=2.0          # eski .xls (BIFF/OLE2) okuma
